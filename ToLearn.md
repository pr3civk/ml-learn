obsługa braków danych w różny sposób (SimpleImputer, IterativeImputer, KNNImputer),

kodowanie zmiennych kategorycznych (OneHot, Ordinal, target encoding),

skalowanie i normalizacja (StandardScaler, MinMaxScaler, RobustScaler),

redukcja wymiarów (PCA, t-SNE, UMAP),

tworzenie nowych cech (feature engineering).

Możesz też ogarniać hyperparameter tuning (GridSearchCV, RandomizedSearchCV, Optuna, scikit-optimize).

Praktyka: testowanie modeli na różnych zestawach cech, walidacja krzyżowa, interpretacja wyników.